// stage_attributes.def — function attributes with stage-entry meaning.

#ifndef RTSL_STAGE_ATTRIBUTE
#define RTSL_STAGE_ATTRIBUTE(attribute, stage)
#endif

RTSL_STAGE_ATTRIBUTE(vertex, vertex)
RTSL_STAGE_ATTRIBUTE(fragment, fragment)

#undef RTSL_STAGE_ATTRIBUTE
