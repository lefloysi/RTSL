// builtins.def — intentionally empty for v0.1.
//
//   RTSL_BUILTIN(name, spelling)
//
// Stage globals are explicit entry parameters, not return-boundary tags.
// `clip` is interpolation/placement metadata, not a builtin named `position`.
//
#ifndef RTSL_BUILTIN
#define RTSL_BUILTIN(name, spelling)
#endif

#undef RTSL_BUILTIN
